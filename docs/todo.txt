## Todo:

implement measuremen of KPIs for different kinds of pollution.

generating logs for different functions to control if the behaviour runs as expected.
For example: Full log for the recovery mechanism. To self check if the kpis are measured correctly.
Or fro the pollution, have a log of the original generation, then the polluted packets and see how the recovery is working on that-
Or for stepwise rref, i want to check if the algorithm is working correctly and see the result.


## Todos if there is enough time left over:

Implement more efficient way of generating non zero tag error packets.


## Questions:

What are reasonable KPIs that are used in related research papers?

Are there other measurements that would make sense?

How can we measure the security of this algorithm?

