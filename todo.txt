
Stand 11.02.2026

Done:
using my own finite field implementation, dont need pyerase for the field anymore 
generating plots with error rates for different field sizes
monte carlo simulation of error rates for all fields

27.02: simple recoding done, orthogonality stays intact 
28.02: started rref implementation, added base functions like inverse that will be necessary
03.03 rref implemented


TODO: 
make repo easily portable / executable on other machines

monte carlo test with recoding

implement swap / other means of preventing the 0 error tag

compare error rates, when swapping

implementing ARC
-> implementing combined recovery


pollution security check for different fields

Later:


optimize performance 

    use functools to cache answer, for example for inverse, square root   etc

    if code is sparse, can maybe save some time with rref implement
    
    Python's built-in ctypes module
    Cython: Write Python-like code 
    CPython Extensions: Pure C API 


    We can swap two numbers without requiring a temporary variable by using the XOR operator (^). 
    It works because applying XOR twice with the same value brings back the original number.
        a = 5
        b = 10
        ​
        a = a ^ b
        b = a ^ b
        a = a ^ b